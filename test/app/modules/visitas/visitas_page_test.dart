import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:interport_app/app/modules/visitas/visitas_page.dart';

void main() {
  testWidgets('VisitasPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(VisitasPage(title: 'Visitas')));
    //  final titleFinder = find.text('Visitas');
    //  expect(titleFinder, findsOneWidget);
  });
}
