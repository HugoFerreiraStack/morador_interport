import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:interport_app/app/modules/livro_ocorrencia/livro_ocorrencia_page.dart';

void main() {
  testWidgets('LivroOcorrenciaPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(LivroOcorrenciaPage(title: 'LivroOcorrencia')));
    //  final titleFinder = find.text('LivroOcorrencia');
    //  expect(titleFinder, findsOneWidget);
  });
}
