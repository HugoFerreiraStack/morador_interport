import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:interport_app/app/modules/comunicados/comunicados_page.dart';

void main() {
  testWidgets('ComunicadosPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ComunicadosPage(title: 'Comunicados')));
    //  final titleFinder = find.text('Comunicados');
    //  expect(titleFinder, findsOneWidget);
  });
}
