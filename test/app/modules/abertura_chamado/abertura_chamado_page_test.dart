import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:interport_app/app/modules/abertura_chamado/abertura_chamado_page.dart';

void main() {
  testWidgets('AberturaChamadoPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AberturaChamadoPage(title: 'AberturaChamado')));
    //  final titleFinder = find.text('AberturaChamado');
    //  expect(titleFinder, findsOneWidget);
  });
}
