import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:interport_app/app/modules/eventos/eventos_page.dart';

void main() {
  testWidgets('EventosPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(EventosPage(title: 'Eventos')));
    //  final titleFinder = find.text('Eventos');
    //  expect(titleFinder, findsOneWidget);
  });
}
