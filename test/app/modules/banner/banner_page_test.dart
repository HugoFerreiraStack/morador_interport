import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:interport_app/app/modules/banner/banner_page.dart';

void main() {
  testWidgets('BannerPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(BannerPage(title: 'Banner')));
    //  final titleFinder = find.text('Banner');
    //  expect(titleFinder, findsOneWidget);
  });
}
