import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:interport_app/app/modules/cameras/cameras_page.dart';

void main() {
  testWidgets('CamerasPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CamerasPage(title: 'Cameras')));
    //  final titleFinder = find.text('Cameras');
    //  expect(titleFinder, findsOneWidget);
  });
}
