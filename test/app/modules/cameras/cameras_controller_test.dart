import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:interport_app/app/modules/cameras/cameras_controller.dart';
import 'package:interport_app/app/modules/cameras/cameras_module.dart';

void main() {
  initModule(CamerasModule());
  // CamerasController cameras;
  //
  setUp(() {
    //     cameras = CamerasModule.to.get<CamerasController>();
  });

  group('CamerasController Test', () {
    //   test("First Test", () {
    //     expect(cameras, isInstanceOf<CamerasController>());
    //   });

    //   test("Set Value", () {
    //     expect(cameras.value, equals(0));
    //     cameras.increment();
    //     expect(cameras.value, equals(1));
    //   });
  });
}
