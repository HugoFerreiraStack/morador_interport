import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:interport_app/app/modules/cad_usuarios/cad_usuarios_page.dart';

void main() {
  testWidgets('CadUsuariosPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CadUsuariosPage(title: 'CadUsuarios')));
    //  final titleFinder = find.text('CadUsuarios');
    //  expect(titleFinder, findsOneWidget);
  });
}
