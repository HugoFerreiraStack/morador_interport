import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:interport_app/app/modules/cad_condominios/cad_condominios_page.dart';

void main() {
  testWidgets('CadCondominiosPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CadCondominiosPage(title: 'CadCondominios')));
    //  final titleFinder = find.text('CadCondominios');
    //  expect(titleFinder, findsOneWidget);
  });
}
